import React from 'react';
// import { Link } from 'react-router';

export const ProjectPreview = ({ children }) => {
    
    return (
        <div className="project">
            {children}
        </div>
    )
}